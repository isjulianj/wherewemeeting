
import React, { FunctionComponent } from "react";



const AddLocations: FunctionComponent = ({ children }) => {

  return (

    <form className='container'>
      {children}
    </form>

  )

};

export default AddLocations;
